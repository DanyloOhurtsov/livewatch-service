import ToggleSidebar from "./toggle";
import { Wrapper } from "./wrapper";

const SideBar = () => {
    return (
        <Wrapper>
            <ToggleSidebar />
        </Wrapper>
    );
};

export default SideBar;
